name: english_pro
description: English Learning App V6
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  pdf: ^3.10.8
  printing: ^5.11.1
  
  # Network & APIs
  http: ^1.2.0
  
  # PDF Generation
  pdf: ^3.10.8
  printing: ^5.11.1
  
  # Utilities
  provider: ^6.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
