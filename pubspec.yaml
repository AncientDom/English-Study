name: afcat_prep
description: English Preparation App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0  # For saving quiz timers and exam results
  provider: ^6.0.0            # For managing state across screens
  intl: ^0.18.0               # For date/time formatting

flutter:
  uses-material-design: true
  
